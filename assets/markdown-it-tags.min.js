"use strict";window.markdownitTags=function(Tags){return function(md){function tagsTokenize(state,silent){let token,start=state.pos,marker=state.src.charCodeAt(start);if(silent){return false}if(marker!==91){return false}const labelStart=state.pos+1;const labelEnd=state.md.helpers.parseLinkLabel(state,state.pos+1,false);if(labelEnd<0){return false}const tagName=state.src.substr(labelStart,labelEnd-1);let tagColor="black";if(Tags.hasOwnProperty(tagName)){tagColor=Tags[tagName]}token=state.push("tag","tag",0);token.attrs=[[tagColor]];token.content=tagName;state.pos=labelEnd+1;return true}md.inline.ruler.push("tags",tagsTokenize);md.renderer.rules.tag=function(tokens,idx,options,env,slf){return`<span class="tag is-${tokens[idx].attrs[0][0]}">${tokens[idx].content}</span>`}}};